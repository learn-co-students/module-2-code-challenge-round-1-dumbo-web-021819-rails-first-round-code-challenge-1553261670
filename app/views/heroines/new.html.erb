<h1>Creating a new Heroine </h1>

<ul>
<% if @heroine.errors.any? %>
    <% @heroine.errors.full_messages.each do |msg|%>
    <li> <%= msg %> </li>
    <% end %>
<% end %>
</ul>

<%= form_for @heroine do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <br>
  <br>
  <%= f.label :super_name %>
  <%= f.text_field :super_name %>
  <br>
  <br>
  Select a Superpower:
  <%= f.collection_select :power_id, Power.all, :id, :name %>
  <br>
  <%= f.submit %>

<% end %>
